[workspace]
members = [
    "crates/voxbox-core",
    "crates/voxbox-ui",
    "crates/voxbox-plugin",
    "crates/voxbox-standalone",
    "crates/voxbox-cli",
]
resolver = "2"

[profile.release]
lto = "thin"
strip = "symbols"
