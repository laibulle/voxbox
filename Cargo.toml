[workspace]
members = [
    "crates/voxbox-core",
    "crates/voxbox-ui",
    "crates/voxbox-plugin",
    "crates/voxbox-standalone",
]
resolver = "2"

[profile.release]
lto = "thin"
strip = "symbols"
